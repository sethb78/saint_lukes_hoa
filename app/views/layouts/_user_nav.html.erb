<p>
	<% if user_signed_in? %>
	 	Signed in as <%= link_to current_user.name, edit_user_registration_path %>. 
	 	<p>Not you?  <%= link_to "Sign Out", destroy_user_session_path %></p>
	 			<% if current_user.admin? %>
			<p><%= link_to "Admin Tools", admin_path, target: "blank" %></p>
			<p><%= link_to "Users List", users_path %></p>
		<% end %>
	<% else %>
		<%= link_to "Register", new_user_registration_path %> or
		<%= link_to "Sign In", new_user_session_path %>
	<% end %>
<p>